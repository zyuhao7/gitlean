# Day 21：使用标签

## 学习目标

给重要版本打标签。

## 核心概念

标签常用于发布版本，例如 `v1.0.0`。它可以固定指向某一次提交。

## 常用命令

```bash
git tag v1.0.0
git tag
git push origin v1.0.0
```

## 今日练习

1. 给当前提交打 `v0.1.0` 标签。
2. 执行 `git tag` 查看标签。
3. 推送标签到远程仓库。

## 检查点

你应该知道：标签适合标记发布版本、里程碑版本。

